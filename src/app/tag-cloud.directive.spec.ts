import { TagCloudDirective } from './tag-cloud.directive';

describe('TagCloudDirective', () => {
  it('should create an instance', () => {
    const directive = new TagCloudDirective();
    expect(directive).toBeTruthy();
  });
});
